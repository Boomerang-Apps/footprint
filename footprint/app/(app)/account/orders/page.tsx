'use client';

import { OrderHistoryList } from '@/components/account/OrderHistoryList';

/**
 * Order History Page
 * Displays list of user's past orders with filtering and statistics
 */
export default function OrderHistoryPage(): React.ReactElement {
  return <OrderHistoryList />;
}